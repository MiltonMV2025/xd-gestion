---
import { SESSION_COOKIE } from "@/utils/navigation";

export const prerender = false;

const hasSession = Boolean(Astro.cookies.get(SESSION_COOKIE)?.value);

return Astro.redirect(hasSession ? "/dashboard" : "/login");
---
